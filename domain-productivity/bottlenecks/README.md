# Bottlenecks & Personal Constraints Prompts

Diagnostics for when deep-work isn't the blocker and something else is. These prompts treat progress as a system with three lanes — clarity, execution, distribution — and force the user to diagnose which one is actually binding before prescribing action.

## When to Use

Use these when:
- Consistent effort is not producing progress on what the user actually wants
- A new habit, course, or sprint feels like it might be aimed at the wrong thing
- Output exists but reach or growth doesn't
- The user keeps rewriting their goals without shipping

Do **not** use these for:
- Focus-system problems (use `../deep-work/`)
- Decision validation (use `../validation/`)
- Pure stuckness with no recent action (use `../../domain-personal-development/prompts/agency/agency_stuck_diagnosis.md`)

## Prompts

- [`bottleneck_locator.md`](bottleneck_locator.md) — **Start here.** Diagnose which of three lanes (clarity / execution / distribution) is the real binding constraint. Routes to the other three prompts.
- [`bottleneck_clarity_ambition_surfacer.md`](bottleneck_clarity_ambition_surfacer.md) — When clarity is binding: produce a one-page, falsifiable articulation of what the user actually wants.
- [`bottleneck_daily_execution_habits.md`](bottleneck_daily_execution_habits.md) — When execution is binding: design 1–3 daily habits producing visible outputs, sized to the user's real calendar.
- [`bottleneck_distribution_constraint_finder.md`](bottleneck_distribution_constraint_finder.md) — When distribution is binding: diagnose which of five distribution causes (audience absent, wrong artifact shape, channel mismatch, trust missing, no ask) is blocking reach.

## Typical Sequence

1. Run `bottleneck_locator.md` first. It picks one lane and names one next move.
2. Follow with the lane-specific prompt.
3. After two weeks of action in the chosen lane, re-run `bottleneck_locator.md` with the new 30-day data. The binding lane usually shifts once the first is relieved.

## Related

- Deep work & focus (if focus is what's broken): [`../deep-work/`](../deep-work/)
- Agency, stuckness, habit repair: [`../../domain-personal-development/prompts/agency/`](../../domain-personal-development/prompts/agency/)
- Decision validation: [`../validation/`](../validation/)
