<!-- SYNTHETIC TEST FIXTURE — NOT INDEPENDENT BENCHMARK EVIDENCE -->

Gate 0 negative fixture. Every markdown file under the configured roots here is
a meta document, so discovery finds nothing and Gate 0 must block rather than
report a clean corpus.
