# AI Investment Research Toolkit — Python dependencies
#
# Stdlib-first by design: in manual-only mode (paste data into data/input/, no
# adapters), NOTHING here is required. The toolkit runs on the standard library.
# Install these only as you turn stubs into real adapters / scripts.

# --- Optional: config parsing (NOT required) ---
# pyyaml          # the scripts use PyYAML if it is installed; otherwise they fall back
#                 # to a small embedded YAML-subset parser, so config/*.yaml and the
#                 # pattern/journal frontmatter parse with zero dependencies.

# --- Optional: data handling, once you wire real adapters (later phases) ---
# pandas          # tabular snapshot / universe handling
# requests        # HTTP for market-data / filings / on-chain adapters

# Phase 6: the four skill scripts are implemented and stdlib-only. Run
#   python -m unittest discover -s tests
# and each script's --self-check with nothing installed beyond the standard library.
